@model IEnumerable<CAMS.Models.Lab>
@{
    ViewBag.Title = "Index";
}

<h2 class="text-center">כיתות מחשוב</h2>

@*<p>
        if limitted acsses or full conrol
               @Html.ActionLink("Create New", "Create")
    </p>*@
@*add here sorting, search and filters*@
<table class="table">
    <tr>
        <td>שם</td>
        <td>בניין</td>
        <td>כיתה</td>
        <td>שיעורים מתוכננים להיום</td>
        <th></th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.LabName)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Building)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.RoomNumber)
            </td>
            <td>
                @*TBD - classes for today*@
            </td>
            <td>
                @*TBD -  only full control can delete and only limited can edit*@
                @Html.ActionLink("Edit", "Edit", new { id = item.LabId }) |
                @Html.ActionLink("Details", "Details", new { id = item.LabId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.LabId })
            </td>
        </tr>
    }
</table>
